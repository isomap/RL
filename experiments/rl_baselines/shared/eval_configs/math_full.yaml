defaults: "../../../../examples/configs/evals/eval.yaml"

generation:
  model_name: null  # Override: generation.model_name=<checkpoint_path>
  vllm_cfg:
    max_model_len: 8192

data:
  prompt_file: "examples/prompts/cot.txt"
  dataset_name: "math"

env:
  math:
    num_workers: 16
